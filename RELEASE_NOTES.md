# GPT 翻译助手 1.1.0

1.1 是截图、划词与设置体验的一次集中升级。

## 主要更新

- 截图编辑器支持连续叠加多次截图；确认最新截图后统一关闭尚未完成的截图窗口。
- 增加自由笔触马赛克、连续粗细滑块、文字点击输入、OCR 直接翻译和翻译源失败顺延。
- 截图取景放大镜同时显示 HEX 与 RGB 色值，并提示及支持 `⌘C` 复制。
- 标注颜色保留常用快捷色，并增加“更多”连续色盘，可拖动圆点无级选色。
- 置顶截图获得焦点后可按 `Esc` 单独关闭，也可继续使用右上角关闭按钮。
- 修复重复启动截图可能卡死的问题，并支持在已有截图上继续取景。
- 修复 WPS PDF 不提供标准辅助功能选区时无法划词翻译的问题；必要时使用复制回退并恢复原剪贴板。
- 修复 Apple 离线翻译对短中文、短英文和品牌词的语言方向误判。
- 设置窗口改为单实例，菜单栏点击设置不再弹出主窗口；全部设置自动保存，移除保存与取消按钮。
- 菜单栏翻译源状态改为紧凑三列，精简状态文字，并统一操作项与快捷键提示样式。

## 安装

下载 `GPT-Translator-1.1.0-macOS.dmg`，打开后将“GPT 翻译助手”拖入 Applications。

当前构建未经过 Apple Developer ID 公证。若 macOS 阻止首次打开，请在 Finder 中右键应用并选择“打开”。Apple 离线翻译需要 macOS 15 或更高版本；其他翻译源仍支持 macOS 13 或更高版本。

使用 OpenAI/ChatGPT OAuth 翻译源前，需要自行安装 Codex CLI 并完成 `codex login`；使用 Gemini/Google OAuth 翻译源前，需要自行安装 Antigravity CLI（`agy`）并完成 Google 登录。应用不会附带 CLI，也不会读取、复制或打包用户的 OAuth Token。

发布包不包含开发者或使用者的 OAuth 登录信息、API Key、钥匙串内容及本机配置。所有使用者需要自行登录或配置翻译服务。
