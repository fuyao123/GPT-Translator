# GPT 翻译助手 0.2.1

本版本加入应用内更新检查，并延续 0.2.0 的 Apple 离线翻译与快捷翻译框功能。

## 主要更新

- 应用启动时自动检查 GitHub Releases 最新正式版本。
- 菜单栏右下角显示更新状态，发现新版时可直接打开下载页。
- 支持手动重新检查，并显示最新版、检查中和失败状态。
- 修复后台钥匙串授权弹窗、Apple 语言包意外下载弹窗。
- 修复快捷翻译框同时唤起主窗口，以及 OCR 选区无法用 `Esc` 取消的问题。

- Apple 离线翻译：系统语言包下载完成后可断网使用，不需要 API Key。
- 快捷翻译框：默认按 `⌘⇧D` 打开，自动判断中英文并互译。
- 快捷框支持拖动、钉住、复制、写回原输入框和最近 20 条本机历史。
- 划词、OCR、快捷翻译框可分别自定义组合键与 A–Z 字母键。
- 菜单栏显示 OCR 与快捷翻译框的当前快捷键，并保留所有已启用来源的连接状态。
- 修复快捷键冲突、OCR 截图方向与重复权限请求、窗口唤起不稳定等问题。

完整记录见 `CHANGELOG.md`。

## 安装

下载 `GPT-Translator-0.2.1-macOS.dmg`，打开后将“GPT 翻译助手”拖入 Applications。

当前构建未经过 Apple Developer ID 公证。若 macOS 阻止首次打开，请在 Finder 中右键应用并选择“打开”。Apple 离线翻译需要 macOS 15 或更高版本；其他翻译源仍支持 macOS 13 或更高版本。

发布包不包含开发者或使用者的 OAuth 登录信息、API Key、钥匙串内容及本机配置。所有使用者需要自行登录或配置翻译服务。
